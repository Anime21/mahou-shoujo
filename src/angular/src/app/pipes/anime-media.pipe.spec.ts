import { AnimeMediaPipe } from './anime-media.pipe';

describe('AnimeMediaPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimeMediaPipe();
    expect(pipe).toBeTruthy();
  });
});
