import { AnimeTypePipe } from './anime-type.pipe';

describe('AnimeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AnimeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
