import { AnimeSitePipe } from './anime-site.pipe';

describe('AnimeSitePipe', () => {
  it('create an instance', () => {
    const pipe = new AnimeSitePipe();
    expect(pipe).toBeTruthy();
  });
});
