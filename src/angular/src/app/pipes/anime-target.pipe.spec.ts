import { AnimeTargetPipe } from './anime-target.pipe';

describe('AnimeTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimeTargetPipe();
    expect(pipe).toBeTruthy();
  });
});
